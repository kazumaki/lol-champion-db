import React from 'react';
import style from '../styles/LockInButton.module.css';

const LockInButton = () => {
  return (
    <button className={style.button}>LOCK IN</button>
  )
}

export default LockInButton;